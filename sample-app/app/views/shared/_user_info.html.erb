<%= link_to gravatar_for(current_user, size: Settings.gravatar.size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("microposts.view"), current_user %></span>
<span><%= t "microposts.count.micropost", count: current_user.microposts.size %></span>
