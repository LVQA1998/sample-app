<% provide :title, t("users.index.title") %>

<h1><%= t "users.index.title" %></h1>
<%= paginate @users, theme: Settings.paginate_theme %>
<ul class="users">
  <%= render @users %>
</ul>
<%= paginate @users, theme: Settings.paginate_theme %>